<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
        http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task-4.3.xsd">


	<!-- Configuracion aplicacion -->
	<context:property-placeholder location="file:#{systemProperties['es.caib.sistramit.properties.path']}"/>

	<!-- Scans the classpath of this application for @Components to deploy as beans -->
	<!--  scoped necesario para que el objeto session info pueda usarse desde controllers (otra opc seria definir exclusivamente sessioninfo como scoped -->
	<context:component-scan base-package="es.caib.sistramit.frontend" scoped-proxy="interfaces"/>

	<!-- Procesos -->
	<task:annotation-driven/>

	<!-- Propiedades -->
	<bean id="frontModuleConfig" class="es.caib.sistramit.frontend.ModuleConfig">
		<property name="version" value="${sistra2.version}" />
		<property name="commitSvn" value="${commitSvn}" />
		<property name="commitGit" value="${commitGit}" />
	</bean>


	<!--  Textos de mensajes (bean accesible desde componente Literales) -->
	<bean id="messageSource"
		class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
		<property name="basename" value="classpath:messages" />
		<property name="defaultEncoding" value="UTF-8" />
	</bean>


	<!--  Configura recursos estaticos -->
	<mvc:resources mapping="/resources/**" location="file:${ficherosExternos.path}/publico/" />
	<mvc:resources mapping="/base/**" location="/base/" />
	<mvc:resources mapping="/asistente/**" location="/asistente/" />
	<mvc:resources mapping="/debug/**" location="/debug/" />

	<!-- MVC annotation -->
	<mvc:annotation-driven />

	<!--  Interceptores -->
	<mvc:interceptors>
		<!-- Añadimos explicitamente headers xa no cache -->
		<bean id="webContentInterceptor"
			class="org.springframework.web.servlet.mvc.WebContentInterceptor">
			<property name="cacheSeconds" value="0" />
			<property name="useExpiresHeader" value="true" />
			<property name="useCacheControlHeader" value="true" />
			<property name="useCacheControlNoStore" value="true" />
		</bean>
	</mvc:interceptors>

	<!-- Idioma sesion -->
	<bean id="localeResolver"
	    class="org.springframework.web.servlet.i18n.SessionLocaleResolver">
	    <property name="defaultLocale" value="es" />
	</bean>


	<!-- View resolver -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
    	<property name="viewClass" value="org.springframework.web.servlet.view.JstlView" />
       <property name="prefix" value="/WEB-INF/jsp/"></property>
       <property name="suffix" value=".jsp"></property>
     </bean>

	<!-- Upload ficheros-->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- En caso de querer habilitar hay que capturar excepcion generada en el ExceptionHandler: org.apache.commons.fileupload.FileUploadException -->
		<property name="maxUploadSize" value="-1"/>
	</bean>

</beans>