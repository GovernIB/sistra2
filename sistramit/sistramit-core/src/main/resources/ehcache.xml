<?xml version="1.0" encoding="UTF-8"?>
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="http://ehcache.org/ehcache.xsd"
	updateCheck="false">
	<diskStore path="java.io.tmpdir" />

	<!-- Cache Configuracion (global/entidad): se cachea excepto si se fuerza mediante invalidacion -->
	<cache
		name="cacheConfiguracion"
		maxElementsInMemory="1000"
	    overflowToDisk="false"
	    diskPersistent="false"
	    timeToIdleSeconds="0"
	    timeToLiveSeconds="0"
	    memoryStoreEvictionPolicy="LRU"/>


	 <!-- Cache definicion tramites: se cachea maximo 24h y entre peticiones 1h (se puede forzar mediante invalidacion)-->
	<cache
		name="cacheTramites"
		maxElementsInMemory="100"
	    overflowToDisk="false"
	    diskPersistent="false"
	    timeToIdleSeconds="3600"
	    timeToLiveSeconds="86400"
	    memoryStoreEvictionPolicy="LRU"/>

	 <!-- Cache definicion dominios: se cachea maximo 24h y entre peticiones 1h (se puede forzar mediante invalidacion)-->
	<cache
		name="cacheDominios"
		maxElementsInMemory="100"
	    overflowToDisk="false"
	    diskPersistent="false"
	    timeToIdleSeconds="3600"
	    timeToLiveSeconds="86400"
	    memoryStoreEvictionPolicy="LRU"/>

 	<!-- Cache datos de dominio: se cachea maximo 24h y entre peticiones 1h (se puede forzar mediante invalidacion)-->
	<cache
		name="cacheDominiosDatos"
		maxElementsInMemory="1000"
	    overflowToDisk="false"
	    diskPersistent="false"
	    timeToIdleSeconds="3600"
	    timeToLiveSeconds="86400"
	    memoryStoreEvictionPolicy="LRU"/>

	<!-- Cache implicita datos de dominio: aunque no se cachee dominio, se cachean datos durante 1 minuto (rendimiento scripts formularios) -->
	<cache
		name="cacheImplicitaDominiosDatos"
		maxElementsInMemory="1000"
	    overflowToDisk="false"
	    diskPersistent="false"
	    timeToIdleSeconds="60"
	    timeToLiveSeconds="60"
	    memoryStoreEvictionPolicy="LRU"/>

	<!-- Cache catalogo procedimientos: se cachea maximo 10 min (se puede forzar mediante invalidacion entidad)-->
	<cache
		name="cacheCP"
		maxElementsInMemory="100"
	    overflowToDisk="false"
	    diskPersistent="false"
	    timeToIdleSeconds="600"
	    timeToLiveSeconds="600"
	    memoryStoreEvictionPolicy="LRU"/>

</ehcache>