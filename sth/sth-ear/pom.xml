<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>es.caib.sistra2</groupId>
		<artifactId>sth</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>
	<artifactId>sth-ear</artifactId>
	<packaging>ear</packaging>
	<name>sth-ear</name>
	<dependencies>
		<dependency>
			<groupId>es.caib.sistra2</groupId>
			<artifactId>sth-lib</artifactId>
			<version>${sth.version}</version>
			<type>pom</type>
		</dependency>
		<dependency>
			<groupId>es.caib.sistra2</groupId>
			<artifactId>sth-core</artifactId>
			<version>${sth.version}</version>
			<type>ejb</type>
		</dependency>
		<dependency>
			<groupId>es.caib.sistra2</groupId>
			<artifactId>sth-frontend</artifactId>
			<version>${sth.version}</version>
			<type>war</type>
		</dependency>
		<dependency>
			<groupId>es.caib.sistra2</groupId>
			<artifactId>sth-rest-ws</artifactId>
			<version>${sth.version}</version>
			<type>war</type>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-ear-plugin</artifactId>
				<version>${maven.ear.version}</version>
				<configuration>
					<finalName>sth</finalName>
					<version>5</version>
					<modules>
						<ejbModule>
							<groupId>es.caib.sistra2</groupId>
							<artifactId>sth-core</artifactId>
							<bundleFileName>sth-core.jar</bundleFileName>
						</ejbModule>
						<webModule>
							<groupId>es.caib.sistra2</groupId>
							<artifactId>sth-frontend</artifactId>
							<contextRoot>/sistra2back/sth-frontend</contextRoot>
							<bundleFileName>sth-frontend.war</bundleFileName>
						</webModule>
						<webModule>
							<groupId>es.caib.sistra2</groupId>
							<artifactId>sth-rest-ws</artifactId>
							<contextRoot>/sistra2back/sth-rest</contextRoot>
							<bundleFileName>sth-rest.war</bundleFileName>
						</webModule>
					</modules>
					<defaultLibBundleDir>lib/</defaultLibBundleDir>
					<skinnyWars>true</skinnyWars>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven.compiler.version}</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<compilerVersion>${java.version}</compilerVersion>
					<encoding>${project.build.sourceEncoding}</encoding>
				</configuration>
			</plugin>
		</plugins>
		<finalName>sth</finalName>
	</build>
</project>