Tras ejecutar 1_sistrages_update_from_1.2.2_to_1.3.0.sql y hacer las pruebas, ejecutar esta sentencia:

Drop table STG_DOMENT cascade constraints;
Drop table STG_AREDOM cascade constraints;


2_SISTRA2_SELECT_INVOCARDOMINIO_FUNCION_isIdentificadorCorrecto.sql y 3_SISTRA2_SELECT_INVOCARDOMINIO_FUNCION_obtenerIdentificadorCorrecto.sql. Estos dos script crean dos funciones PL/SQL que utilizará el script siguiente.

4_SISTRA2_SELECT_INVOCARDOMINIO_AVANZADO.sql. Este script devuelve los registros de BBDD que deberán modificarse con los identificadores compuestos, referenciados en los scripts de los trámites que contienen invocaciones a dominios.

5_CREACION_UPDATE_ROLSAC.sql. 